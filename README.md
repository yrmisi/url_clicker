# 🐳 Clicker Url - Короткие ссылки 🚀

**Быстрый URL-шортенер с rate limiting и подсчетом кликов!**
Современный сервис укорачивания ссылок на FastAPI + PostgreSQL + Redis в Docker.

## 📋 Быстрый старт

```bash
# Клонируем и запускаем
git clone <repo>
cd clicker_avito
docker compose up -d

# Проверяем работу
curl -X POST "http://localhost/api/short_url" \
  -H "Content-Type: application/json" \
  -d '{"long_url": "https://avito.ru"}'
```

## 🏗️ Архитектура проекта

```text
├── 📁 src/                 # Основной код FastAPI
│   ├── 🗄️ database/        # SQLAlchemy модели и CRUD
│   ├── ⚙️ services/        # Бизнес-логика (slug, rate limit)
│   ├── 🛡️ dependencies/    # Rate limiting
│   └── 🧰 utils/           # Валидация URL, подсчет кликов
├── 🐘 postgres_data/       # База данных
├── 🟥 redis_data/          # Redis для rate limiting
├── 🌐 frontend/            # Статический nginx + HTML
├── 🧪 tests/               # Тесты API и сервисов
└── 🐳 docker-compose.yaml  # Всё в контейнерах!
```

## 🔌 API endpoints

### `POST /api/short_url` 🆕

**Создать короткую ссылку** <!-- markdownlint-disable-line MD036 -->

```bash
curl -X POST "http://localhost/api/short_url" \
  -H "Content-Type: application/json" \
  -d '{"long_url": "https://example.com/very/long/url"}'
```

**✅ Ответ:**

```json
{
  "link": "http://localhost/api/abc123",
  "creation_count": 1
}
```

**🔒 Rate limit:** Защита от спама через Redis

### `GET /api/{slug}` ➡️

**Редирект на оригинал** <!-- markdownlint-disable-line MD036 -->

```text
GET /api/abc123 → 302 → https://example.com/very/long/url
```

**❌ Ошибки:**

- `404` - ссылка не найдена
- `422` - невалидный URL
- `500` - не удалось сгенерировать уникальный slug (5 попыток)

## 🛠️ Особенности

- ✅ **Генерация уникальных slug** - до 5 попыток при коллизиях
- ✅ **Валидация URL** - только корректные ссылки
- ✅ **Rate limiting** - Redis защита от злоупотреблений
- ✅ **Подсчет кликов** - `creation_count` в ответе
- ✅ **Async PostgreSQL** - SQLAlchemy + asyncpg
- ✅ **Docker Compose** - postgres + redis + fastapi + nginx
- ✅ **Alembic миграции** - автоматическое создание таблиц
- ✅ **Pytest тесты** - покрытие API и сервисов

## 🧑‍💻 Локальная разработка

```bash
# Миграции БД
docker compose exec api alembic upgrade head

# Тесты
docker compose exec api pytest

# Логи
docker compose logs -f api
```

## 🔍 Структура БД

```sql
-- Таблица short_urls - основная таблица коротких ссылок
CREATE TABLE short_urls (
    id SERIAL PRIMARY KEY,
    slug VARCHAR(6) PRIMARY KEY,                    -- 🆔 Уникальный короткий код (6 символов)
    long_url VARCHAR(2048) NOT NULL,               -- 🔗 Оригинальная длинная ссылка
    creation_count INTEGER NOT NULL DEFAULT 1,     -- 📊 Количество созданий (для дубликатов)
    click_count INTEGER NOT NULL DEFAULT 0,        -- 👆 Количество кликов/редиректов
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), -- 📅 Дата создания
    last_accessed_at TIMESTAMPTZ                   -- ⏰ Последний клик (nullable)
);
```

### 📋 Поля модели `ShortURL`

| Поле              | Тип          | Описание                              | Default |
|-------------------|--------------|---------------------------------------|---------|
| `slug`            | `VARCHAR(6)` | ✅ **PK** Уникальный короткий код     | -       |
| `long_url`        | `VARCHAR(2048)` | Полная целевая ссылка              | -       |
| `creation_count`  | `INTEGER`    | 🔄 Сколько раз создана эта ссылка     | `1`     |
| `click_count`     | `INTEGER`    | 📈 Количество переходов по ссылке     | `0`     |
| `created_at`      | `TIMESTAMPTZ`| Дата/время создания                   | `NOW()` |
| `last_accessed_at`| `TIMESTAMPTZ`| Последний переход (nullable)          | `NULL`  |

### 🎯 Особенности

- **6-символьный slug** - компактный и читаемый `[a-zA-Z0-9]`
- **Подсчет дубликатов** - `creation_count` растет при повторных сокращениях
- **Аналитика кликов** - `click_count` + `last_accessed_at` для статистики
- **Timezone-aware** даты для корректного логирования
- **VARCHAR(2048)** для long_url - покрывает все практические случаи

**💾 Миграция:** Уже создана в `alembic/versions/2025_11_25_2356-c2ec002c8cf6_create_table_short_urls.py` 🚀

## 🚀 Деплой

1. **Скопируйте конфиги** (`.env` если нужен)
2. **Поднимите сервисы:**

   ```bash
   docker compose up -d --build
   ```

3. **Примените миграции:**

   ```bash
   docker compose exec api alembic upgrade head
   ```

4. **Готово!** API на `http://localhost/api`

## 🐛 Частые проблемы

| ❌ Проблема | ✅ Решение |
| ------------- | ----------- |
| `postgres_data [error opening dir]` | `sudo chown -R 999:999 postgres_data` |
| Slug не генерируется | Проверьте Redis: `docker compose logs redis` |
| 404 на существующий slug | `docker compose exec api alembic upgrade head` |
| Rate limit бьет | Проверьте `redis_data/dump.rdb` |

## 📊 Метрики

- **5 попыток** генерации уникального slug
- **Async/await** everywhere
- **uv.lock** для dependency pinning
- **FastAPI** с автодоками (`/docs`)

## ❤️ Лицензия

```text
MIT License - используйте на здоровье! 🚀
```

***

**Автор:** yrmisi
**🐳 Версия Docker stack:** v29.1.5
**📅 Создано:** Ноябрь 2025
